// Main theme configuration
export { mantineTheme } from './mantineTheme';

// Theme utilities and hooks
export { useThemeColors, getThemeColor, colorCombinations, cssVariables } from './themeUtils';

// Example component (for development/testing)
export { ThemeExample } from './ThemeExample';